Add `"hubspot"` to the `hiddenIntegrationIds` set in `src/pages/Integrations.tsx` (line 226) so the HubSpot card is filtered out of `baseVisibleIntegrations` and no longer rendered. All HubSpot backend wiring and integration metadata remain untouched — only the card is hidden, matching the existing pattern used for `azure-blob`, `basecamp`, `azure-devops`, and `github`.